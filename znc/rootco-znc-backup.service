[Unit]
Description=Backup /var/lib/znc/.znc to k2so.dyn.rootco.de

[Service]
Type=oneshot
ExecStart=/usr/bin/rsync -a /var/lib/znc/.znc root@k2so.dyn.rootco.de:/backups/{{ salt['grains.get']('id') }}/
