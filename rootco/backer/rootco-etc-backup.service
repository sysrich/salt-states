[Unit]
Description=Backup /etc to k2so.dyn.rootco.de

[Service]
Type=oneshot
User=backerclient
ExecStart=/usr/bin/rsync -a /etc backerr@k2so.dyn.rootco.de:/backups/rsync/{{ salt['grains.get']('id') }}/
